# Don't push scripts folder (git hooks / install scripts stay local)
scripts/

# Secret keys and config with credentials (rule violations / security)
wp-config.php
wp-content/wp-defender-secrets.php
.env
.env.*
*.pem
*.key
*.p12
*.pfx

# Defender/firewall logs (sensitive or noisy)
wp-content/uploads/wp-defender/
